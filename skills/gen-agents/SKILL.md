---
name: gen-agents
layer: 工程协作
description: >
  开始在任何项目上工作时触发，当项目没有 AGENTS.md 时触发，
  当用户需要建立项目级 AI 协作基准时触发，当加入有现有规范的项目时触发。
  建立 AI 在这个项目里的行为基准。
interfaces:
  upstream: [identity, principles, project-structure]
  downstream: []
---

## 这个 skill 做什么

建立 AI 在这个项目里的行为基准。

解决的根本问题：全局规范是个人资产，项目协作需要共享基准。
根据所有权和团队情况，决定基准以什么形式在这个项目里生效。

生成的 AGENTS.md 是 skill 的调度入口——
AI 读到它时知道在什么情况下去读取哪个项目内本地 skill，不依赖网络，不依赖仓库可见性。

**内容判断标准，缺一不可：**

1. 这句话会改变 AI 的行为吗？
2. 这个信息会纠正 AI 的错误默认假设，或者填补 AI 在没有代码时的信息空白吗？

两个都回答不了的，不属于这里。

---

## When Not to Use

以下情况不触发本 skill：

- 项目已有完整 AGENTS.md 且内容仍然有效，用户没有表示需要更新
- 用户只是在问关于 AGENTS.md 或 AGENTS 规范的问题，没有生成意图
- 用户明确说只需要参考不需要生成（如"给我看个例子"）

---

## 强制停止点

以下问题的答案必须来自用户，不能 ASSUMPTION 推进：

- 用户在这个项目里是否有所有权（决定规范并让他人遵守）
- 项目是否有现有代码库
- 团队成员是否有统一的全局规范基准
- 语言约定（注释、日志、commit message 分别用什么语言）
- 命令和权限约定
- 这个项目要带入哪些 skill
- 本地全局 skill 源目录路径是什么
- 项目内 skill 目录是否按 `.agents/skills/` 落地

本地全局 skill 源目录不能靠运行时猜测。
如果用户没有特别说明，则使用安装阶段已经写入当前环境的默认本地源目录。
不得自行假设其他 home 路径、工具目录或远程地址。



## 本地 skill 源目录规则

项目级 skill 默认从本地全局 skill 源目录复制。

这个源目录不是运行时临时猜测的，
也不是依赖项目文件里对 `~` 的展开，
而是在安装阶段由安装脚本写入当前环境的真实绝对路径。

规则如下：

- 仓库中的模板可以使用路径占位符
- 安装后的实际环境中，必须使用已展开的绝对路径
- `gen-agents` 在复制项目 skill 时，应以当前环境中已安装的本地全局 skill 源目录为准
- 如果当前环境中的实际源目录与默认值不一致，必须由用户明确说明
- 不得将项目 skill 改为远程 URL 引用
- 不得自行假设其他 home 目录或工具目录
- 复制单位是整个 skill 目录，而不是单个 `SKILL.md` 文件

项目级 skill 的复制来源，是当前环境中已安装的本地全局 skill 源目录。
这个目录应在安装阶段写成真实绝对路径，不依赖运行时对 `~` 的展开。

---

## 执行逻辑

```
第一步：确认所有权（必须问，不能推断）
    有所有权 → 走 Layer 2 路径
    没有所有权 → 走 Layer 3 路径

Layer 2 路径：
    第二步：确认项目状态（必须问）
        空项目 / Vibe Coding 起点 → 收集项目参数
        有代码库 → 从代码推断，只补推断不出或方向错的部分
    第三步：确认团队成员是否有全局规范基准（必须问）
        没有 → 自包含模式，选择要带入项目的 skill
        有 → 偏差模式，只写偏差；如用户要求项目自包含，仍可选择复制 skill
    第四步：确认项目要带哪些 skill（必须问）
    第五步：确认语言约定（必须问）
    第六步：确认命令和权限（必须问）
    第七步：识别 Layer 0 缺口（展示完整规则清单，不预筛选）
    第八步：把选中的 skill 从本地 dotfiles 复制到项目 `.agents/skills/`
    第九步：生成，自检，交付

Layer 3 路径：
    第二步：确认是否有团队规范（必须问）
    第三步：确认是否需要个人本地 skill 副本（必须问）
    第四步：生成对应形态，交付
```

---

## 必须问的信息

所有问题必须等用户回答后才能继续，不能推断，不能跳过。

**第一步——所有路径：**

> "在这个项目里，你能决定工程规范并让其他人遵守吗？"

**Layer 2——第二步：**

> "项目现在有没有代码库？还是从零开始？"

**Layer 2——第三步：**

> "团队成员有没有统一的全局规范基准？如果没有，是否把核心 skill 一起带进这个项目，让它自包含？"

**Layer 2——第四步：**

> "这个项目要带哪些 skill？
> 常见基础组合是：
>
> - identity
> - principles
> - project-structure
> - vibe-plan
> - docs
>   也可以按项目需要额外带：
> - notes-protocol
> - notion-manager
> - decision-record
> - assumption-audit
> - conversation-to-spec
>   你要带哪些？"

**Layer 2——第五步：**

> "语言约定是什么？代码注释 / 日志输出 / 表注释 / 错误码描述 / Git commit message 分别用中文还是英文？"

**Layer 2——第六步：**

> "命令和权限怎么约定？
>
> - AI 可以不问直接执行的：
> - 必须先问用户确认的：
> - 永远不能做的："

**Layer 2——第七步之前：**

> "项目级 skill 将复制到 `.agents/skills/`，生成的 AGENTS.md 会引用这些相对路径。
> 我将从当前环境中已安装的本地全局 skill 源目录复制 skill。
> 这个源目录在你当前环境中可用吗？
> 如果你这台机器上的实际源目录不是默认安装值，请明确告诉我实际路径。"

**Layer 3——第三步：**

> "虽然你没有项目规范所有权，但你要不要为自己的本地工作层带一组项目内 skill 副本？
>
> - 要：生成 AGENTS.local.md，并把选中的 skill 复制到项目 `.agents/skills/`
> - 不要：只生成本地补丁文件，不复制 skill"

---

## Layer 2 路径：有所有权

### 空项目 / Vibe Coding 起点

没有代码，AI 无从推断。以下信息是行为参数，必须收集：

- **技术栈**：决定 principles 的语言落地方式
- **业务域**：防止 AI 用错误语义命名
- **项目阶段**：MVP 可以技术债标注后推进，生产系统不行

先触发 `project-structure` skill 确认目录骨架，再生成 AGENTS.md。
顺序不能反——目录结构是后续所有生成的基准。

### 有代码库

从代码推断技术栈、目录结构、业务域。推断内容用 `[INFERRED]` 标注。
只补两类内容：

- AI 推断不出来的（隐性约定、合规要求、外部接口规则）
- AI 推断方向错的（非主流选型、业务词汇的特殊语义）

不重复 AI 已经能从代码里看到的东西。

### 自包含模式（团队成员没有全局规范基准）

不再使用远程 URL fetch skill。

改为从本地 dotfiles skill 源目录中，按用户选择，把 skill 目录复制到项目内：

- 目标目录固定为：`.agents/skills/`
- 每个 skill 按目录复制，不只复制 `SKILL.md`
- skill 内的 `references/`、`scripts/`、`assets/` 等本地资源一并复制
- 生成的项目 `AGENTS.md` 只引用项目内相对路径

推荐基础组合：

- `identity`
- `principles`
- `project-structure`
- `vibe-plan`
- `docs`

如项目需要，可额外带入其他 skill。

**本地 skill 源目录不可用时：**

告知用户无法从本地 dotfiles 复制 skill，说明两个选项及其含义：

> "无法从本地 dotfiles 复制 skill。有两个选项：
>
> 1. 手动提供要带入的 skill 内容，我按项目目录结构整理后继续（项目仍可自包含）
> 2. 只生成不带 skill 副本的 AGENTS.md（这意味着项目只有规则入口，没有本地 skill 实体）
>    选哪个？"

不能静默降级，不能假装已经复制完成，不能自行决定改成不自包含模式。

### 偏差模式（团队成员已有全局规范基准）

只写这个项目相对团队全局规范的偏差。

默认不需要复制全量基础 skill。
但如果用户明确要求项目仓库自包含，或者团队协作环境无法假定所有人都已有同一套全局 skill，则仍可按用户选择复制 skill 到项目 `.agents/skills/`。

区分标准：

- **偏差模式**：团队已有统一基准，项目文件只写偏差
- **自包含模式**：项目自身携带所需 skill，协作者或 AI 工具不依赖用户 home 目录和私有仓库可见性

### 识别 Layer 0 缺口

**缺口一：哪些全局规则在这个项目里不成立？**

展示完整规则清单，不预筛选，让用户自己判断：

> "全局规范里有以下默认规则，哪些在这个项目里需要调整？
>
> - 不用 dict / Map 作为返回类型
> - 业务逻辑和 IO 分离
> - 敏感字段进日志前必须脱敏
> - 权限校验统一在入口层
> - 命名禁止 Manager / Handler / Helper / Processor
> - 接口变更必须向后兼容或显式版本化
> - 发现命名漂移立刻停下来等用户决定
>   没有例外就不写这节。"

**缺口二：这个项目有什么全局规范没覆盖的硬约束？**

> "这个项目有没有 AI 从代码里推断不出来的特殊规则？
> 比如：某类操作必须经过特定检查、某类数据有合规要求、某个接口有安全边界。"

区分标准：

- ✓ "任何 SQL 执行前必须通过 guardrail 检查"——行为约束，AI 不看代码不会知道
- ✗ "这个项目用了 DuckDB"——AI 读 requirements.txt 就能知道，有代码库时写进来是噪音

---

## 项目级 skill 复制规则

项目级 skill 目录固定为：

`.agents/skills/`

项目级 skill 的复制来源，是当前环境中已安装的本地全局 skill 源目录。
这个目录应在安装阶段写成真实绝对路径，不依赖运行时对 `~` 的展开。

复制规则：

- 从本地全局 skill 源目录复制整个 skill 文件夹
- 目录名保持与源 skill 一致
- 不只复制 `SKILL.md`
- skill 目录里的本地依赖资源必须一并复制
- 已存在同名 skill 时，先告知用户是否覆盖
- 复制完成后，项目 `AGENTS.md` 中一律使用相对路径：
  - `./.agents/skills/{skill-name}/SKILL.md`

不要在项目 `AGENTS.md` 中写任何远程 raw GitHub URL。
不要让项目 `AGENTS.md` 依赖用户 home 目录的绝对路径。
项目级副本存在时，项目级 skill 优先于全局 skill。

---

## 输出结构

只有内容才有节，没有内容不生成空节。
`[OPEN QUESTION]` 和 `[ASSUMPTION]` 就近挂在对应条目下，解决了就地删。
不写死用户机器上的绝对路径——项目文件里只写项目内相对路径。

```
# AGENTS.md · [项目名]

## Skill 调度

以下 skill 按触发条件加载。读取项目内本地 skill 失败时告知用户。

### 全局基准

| 触发条件 | Skill |
| -------- | ----- |
| 开始任何任务时 | ./.agents/skills/identity/SKILL.md |

### 工程执行

| 触发条件 | Skill |
| -------- | ----- |
| 写代码 / 命名 / 设计接口时 | ./.agents/skills/principles/SKILL.md |
| 生成任何文件或目录之前 | ./.agents/skills/project-structure/SKILL.md |
| 开始新功能或新模块之前 | ./.agents/skills/vibe-plan/SKILL.md |

### 工程协作

| 触发条件 | Skill |
| -------- | ----- |
| 写任何文档时 | ./.agents/skills/docs/SKILL.md |
| 记录架构 / 流程 / 实现策略的选择时 | ./.agents/skills/decision-record/SKILL.md |

### 认识捕捉

| 触发条件 | Skill |
| -------- | ----- |
| 记笔记 / 整理认识 / 捕捉对话理解时 | ./.agents/skills/notes-protocol/SKILL.md |

### 认识校准

| 触发条件 | Skill |
| -------- | ----- |
| 判断需要被审查 / 结论强于证据时 | ./.agents/skills/assumption-audit/SKILL.md |
| 对话接近承诺时刻 / 感觉差不多了但还没落地时 | ./.agents/skills/conversation-to-spec/SKILL.md |

## 项目参数
[仅空项目 / Vibe Coding 起点生成]
技术栈：
业务域：
项目阶段：

## 语言约定
代码注释：
日志输出：
表注释 / 字段注释：
错误码描述：
Git commit message：

## 命令和权限
允许直接执行：
必须先问：
永远不做：

## Layer 0 在这里的例外
[每条说明原因]
// DEVIATION: 原因

## 这个项目独有的行为约束
[每条是行为指令：在什么情况下，做什么，为什么]
[只写 AI 从代码库里推断不出来或推断方向错的约束]
```

**补充规则：**

- Skill 调度节只写**实际带入项目的 skill**
- 未复制进项目的 skill，不要在表里伪造路径
- 如果项目选择的是偏差模式且不复制 skill，则不生成项目 skill 调度节；只保留项目规则部分
- 如果项目已存在自己的 `.agents/skills/`，以项目内现有副本为准，避免无提示覆盖

**自检（AI 执行）**

- [ ] 每一条能否回答"在什么情况下，AI 做什么不同于默认行为的事"？
- [ ] 有没有把 AI 能从代码正确推断出来的内容写进来？
- [ ] 有没有把未复制进项目的 skill 写进调度表？
- [ ] 有没有写远程 raw URL？
- [ ] 有没有写用户机器上的绝对路径？
- [ ] 本地 skill 缺失时有没有静默降级？
- [ ] 项目级 skill 副本存在时，是否优先于全局 skill？

---

## Layer 3 路径：没有所有权

**情况 A：有团队规范，打补丁**

```
# AGENTS.local.md · [项目名] · 个人补丁

## 覆盖团队规范的地方
[每条说明原因]
// DEVIATION: 原因

## 团队规范没说但需要的行为约束
[每条是行为指令：在什么情况下，做什么，为什么]
```

**情况 B：没有规范，也没权建**

```
# AGENTS.local.md · [项目名] · 个人工作层

[临时文件，等正式规范建立后迁移废弃]

## 在这个项目里的工作方式
[不是项目规范，是全局规范在这个项目里的个人具体化]
[同样只写行为指令，不写项目描述]
```

**两种情况都适用**

临时妥协表不在初始生成物里。遇到"违反 principles 但改不动"时手动追加：

```
## 临时妥协表
| 位置 | 违反了什么 | 原因 | 计划 |
|------|-----------|------|------|
```

**Layer 3 的硬约束**

不进仓库。放项目根目录，加进 `.gitignore`。
没有 `.gitignore` 时用 `.git/info/exclude`。

如果用户明确要求自己的本地工作层也带 skill 副本，则同样复制到项目 `.agents/skills/`，但 `AGENTS.local.md` 仍然不进仓库。

---

## 生成完成后

**Layer 2：**

> "已生成 AGENTS.md。
> 如果你选择了项目自包含模式，所选 skill 已复制到项目 `.agents/skills/`，项目 AGENTS.md 已改为引用这些相对路径。
> 后续新增 skill 时，继续按同样方式复制进项目并补充调度表。
> 随项目推进发现新缺口，直接追加到对应节。
> 开始第一个功能前，触发 vibe-plan 对齐目标和边界。"

**Layer 3：**

> "已生成 AGENTS.local.md，放项目根目录，确认已加进 .gitignore。
> 如果你选择了本地 skill 副本，已复制到项目 `.agents/skills/`，但这些只服务你的本地工作层，不代表项目团队规范。
> 遇到违反 principles 但改不动的地方，追加到临时妥协表。
> 等正式规范建立后，把有价值的条目迁移进去，这个文件可以废弃。"

---

## Failure Modes

以下情况属于错误执行：

- 必须问的信息（所有权、技术栈、skill 列表、语言约定、命令权限）没有收到就开始生成
- 把远程 raw GitHub URL 写进 skill 路径，而不是项目内相对路径
- 把用户机器上的绝对路径硬写进项目 AGENTS.md
- 本地 skill 不存在时静默跳过，没有告知用户并给出两个选项
- 把未复制进项目的 skill 写进调度表，伪造不存在的路径
- 在偏差模式下生成了完整 skill 副本，而用户没有要求项目自包含