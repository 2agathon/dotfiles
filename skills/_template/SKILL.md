---
name: skill-template
layer: 全局基准 / 工程执行 / 工程协作 / 认识捕捉 / 认识校准
description: >
  新建 skill 时使用。复制到新 skill 目录，填写各字段。
  约束执行型 skill 直接复制正文结构；
  协议交互型 skill 只复制 frontmatter，正文自定义。
  不作为独立 skill 加载，仅作为新 skill 的起点。
interfaces:
  upstream: []
  downstream: []
---

## 使用前判断 skill 类型

**约束执行型**：这个 skill 约束 AI 在完成某类任务时的行为。
适用模板的完整结构：强制停止点 / 信息收集 / 自检清单。
例：principles、vibe-plan、docs、project-structure

**协议交互型**：这个 skill 定义一种认识或交互过程。
使用模板的元数据字段（layer、interfaces、description），正文结构自定义。
例：conversation-to-spec、assumption-audit、notes-protocol、decision-record

两种类型都必须填写：layer、interfaces、description。
其余结构按类型选择，不强制统一。



## 这个 skill 做什么

[一句话说清楚这个 skill 的职责。]

---

## 这个 skill 的边界

**适用：** [明确的触发场景]
**不适用：** [明确的排除场景]
**遇到不适用的场景：** [执行什么动作]

---

## 强制停止点

以下情况 AI 必须停下来，等用户确认后才能继续——不能 ASSUMPTION 绕过：

- [情况一：具体描述，说清楚为什么这个判断权在用户]
- [情况二]

> "[停下来时说给用户的话，直接可用]"

---

## 信息收集

**必须来自用户，不能 ASSUMPTION 推进：**
[列出哪些信息的判断权在用户，AI 不能自行推断后继续]

**可以从代码或对话推断，用 `[ASSUMPTION]` 标注：**
[列出哪些信息 AI 可以推断，推断后继续，用户可随时纠正]

**信息来源优先级：**
1. 用户直接说明 → 直接用
2. 用户已在对话里描述过 → 提取后向用户确认
3. 可以合理推断 → `[ASSUMPTION]` 标注，继续推进
4. 无法推断且必须由用户决定 → 停下来问

**信息缺口处理：**

| 情况                   | 处理方式                             |
| ---------------------- | ------------------------------------ |
| 必须来自用户的信息缺失 | 停下来问，只问最影响方向的一个问题   |
| 可以推断的信息缺失     | `[ASSUMPTION]` 标注，继续推进        |
| 无法推断的非关键信息   | `[OPEN QUESTION]` 标注，生成占位内容 |

---

## 执行逻辑

[内部判断逻辑，不是说给用户听的。用条件分支描述，说清楚什么情况下走哪条路。]
```
如果 [条件]：
    执行 [动作]
否则：
    执行 [动作]
```

---

## 核心内容

[每条规则按以下结构写]

### [规则名称]

[规则内容，祈使句]

**这条规则在保护什么**
[保护的是什么，对抗的是什么风险]

**不遵守会发生什么**
[具体的、可感知的后果，不是"质量下降"]

**什么情况下可以偏离**
[明确的例外场景；没有例外就写"无合理偏离场景"]

**偏离时记录**
```
// DEVIATION: 原因
```

**自检（AI 执行，发现违反时的动作）**
- [ ] [具体问题，可以直接在代码或输出里找到答案。违反时执行什么——停下来 / 列出来 / 等用户确认]

---

## 边界

[这个 skill 不处理什么。遇到边界情况执行什么动作。]

---

## 标注体系（所有 skill 统一）

| 标注                    | 用途                       | 谁来解决                  |
| ----------------------- | -------------------------- | ------------------------- |
| `[ASSUMPTION: 内容]`    | 实现细节的推断，可继续推进 | AI 推进，用户可随时纠正   |
| `[OPEN QUESTION: 问题]` | 未解决问题，不能绕过       | 必须由用户决定            |
| `[INFERRED]`            | 从文件或上下文推断的内容   | AI 推进，用户可随时纠正   |
| `// FIXME: 原因 + 计划` | 临时妥协，上线前必须处理   | AI 标注，用户决定何时处理 |
| `// TODO: 内容`         | 已知待做项                 | AI 标注，用户决定何时处理 |
| `// DEVIATION: 原因`    | 有意偏离规范               | AI 说明，用户知情         |

**`[ASSUMPTION]` 的硬边界：只适用于实现细节。目标、边界、权限类问题不能 ASSUMPTION，必须等用户回答。**

---

## 新建 skill 的检查清单

**元数据：**
- [ ] `layer` 字段已填写，选择：全局基准 / 工程执行 / 工程协作 / 认识捕捉 / 认识校准
- [ ] `interfaces.upstream` 已填写，没有上游则填 `[]`
- [ ] `interfaces.downstream` 已填写，没有下游则填 `[]`
- [ ] AGENTS.md 的对应分组已添加触发条件

**触发和边界：**
- [ ] description 只写触发条件，不混入行为指令
- [ ] description 里没有使用 skill body 才能理解的术语
- [ ] 有明确的"不适用"场景
- [ ] 是全局常驻还是触发型，已明确声明

**权力边界：**
- [ ] 哪些信息必须来自用户、不能 ASSUMPTION——已明确列出
- [ ] 强制停止点已明确，停止条件来自规则而不是 AI 的判断
- [ ] 自检里有违反时的具体动作（停下来 / 列出来 / 等用户确认），不只是 AI 问自己"我做了吗"
- [ ] 没有给 AI 留下可以绕过所有确认流程的后门

**表达：**
- [ ] 执行指令全部用祈使句，没有歧义主语
- [ ] 说给用户的话用引号块标出，和执行指令区分开
- [ ] 执行逻辑和说给用户的话已分开

**完整性：**
- [ ] 执行时序清楚：边界 → 强制停止点 → 信息收集 → 执行逻辑 → 核心内容 → 边界
- [ ] 每条规则都有：保护什么 / 后果 / 偏离条件 / 偏离记录 / 自检
- [ ] 自检挂在对应规则下，每条不超过两个问题